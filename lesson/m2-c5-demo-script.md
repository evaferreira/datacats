First, let's understand how our project is organized. We have a main package.json, which is the one that runs the backend and the frontend together. Then, we have the backend one with its dependencies and another one for the dependencies of the frontend.
For this exercise, let's not worry about the one in the root. Let's focus on both backend and frontend.
Let's begin by asking our AI agent for a dependency audit on both package.json files. For each dependency it needs to tell us what the current version is, whether it is out of date or deprecated, or perhaps unmaintained. And we also want to know if there are any known security concerns and how complex an upgrade would be, even if it’s just an approximation.
This will take a few minutes. It does take time to go through all the packages, search for the latest published version in the npm registry, compare it with our repository and run security audits on them.
Alright, it has finished. It found a few packages that are out of date and another few that have known security issues. For example, express has a security vulnerability and moment is no longer maintained, so we should move away from it.
And on the front-end side, there are also some vulnerabilities and some outdated dependencies. For example, a few React testing ones. There’s also a mention of Bootstrap, which right now is in version four, and it's already marked as End of Life, so it should be upgraded to version 5.
As it mentions at the end of the audit, it recommends upgrading Express to clear those security issues, updating react-scripts — the old Create React App setup — and replacing the libraries that are already legacy and end of life, like moment and Bootstrap 4, even if they don't have active CVEs.
Before we act on any of this, one quick recommendation: let's always double-check the version numbers our agent suggests, so we can be sure they actually exist and aren't hallucinated. A quick look at the npm registry takes only seconds, and it saves us from chasing a version that maybe was never published.
Our next step is going to be to move away from moment and replace it with something different, since that library is no longer maintained.
This time, I did some basic research, so I am considering using a library called Day.js, or another one called date-fns. There's also a native alternative that we could use.
I will ask Claude to research them all and give me the pros and cons. I want it to go through our codebase, find any feature gaps, and weigh those against my needs — and then make a call: recommend one of them and explain the tradeoffs. Let's make sure it captures the way we currently use moment, so we can keep the same formatted outputs and avoid any regressions. And since this is only a research task, I'll be clear: please do not modify any code in the process. This will take a few minutes while it works through all of that.
Alright, so here we have our response. It explains the current uses, the edge cases and the different libraries that we proposed with their pros and cons. And at the end it mentions that what it recommends is using Day.js because it has an identical syntax, it’s lightweight and well maintained.
I say, let’s do it! Let’s replace moment with Day.js.
And after a couple of minutes, the swap is done!
There's one last thing I want us to take away, and it might be the most important one: we can make this audit repeatable. Going back to our original session, let's ask the agent to turn what we just did into a reusable skill — a saved set of instructions we can run every quarter. We'll have it capture the same checklist we used today: the current version of each dependency, whether it's out of date, deprecated, or unmaintained, any known security concerns, and how complex an upgrade would be.
That skill lives right on our machine, so from now on the whole audit is a single command. Whenever we want it, we just run /dependency-audit, or simply ask for a dependency audit, and the agent picks it up and runs it. The audit becomes a skill, and the skill becomes a habit.
Give it a shot!
