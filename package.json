{
  "name": "datacats",
  "version": "8.2.1",
  "private": true,
  "description": "DataCats Analytics Dashboard — legacy demo app.",
  "scripts": {
    "postinstall": "npm install --prefix backend && npm install --prefix frontend",
    "start": "concurrently -n backend,frontend -c blue,green --kill-others-on-fail \"npm start --prefix backend\" \"npm start --prefix frontend\"",
    "start:backend": "npm start --prefix backend",
    "start:frontend": "npm start --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "9.2.1"
  }
}
