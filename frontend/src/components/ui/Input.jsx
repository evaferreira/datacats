import React from 'react'

// STUB — not actually usable yet.
// Doesn't support label, error, hint, disabled, or controlled value handling.
// Don't use until v9.
export default function Input(props) {
  return <input {...props} />
}
